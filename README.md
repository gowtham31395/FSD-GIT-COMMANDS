git init
git config --global user.name"Kl24000301395"
git config --global user.email"gowthambobbbadi2526@gmail.com"
git add ("file name")
git commit -m "file name"
git branch -M main
git remote add origin (link)
git push -u orgin main
git checkout
git log# FSD-GIT-COMMANDS
